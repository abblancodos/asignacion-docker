FROM python:3.11.13-alpine3.22

# Agregar las dependencias necesarias de alpine
RUN apk update && apk add --no-cache \
    build-base \
#   ... \

# Agregar las dependencias de python
RUN pip install jupyter \
#   ... \

# Seguir pasos del punto 3. Configuración de Jupyter
WORKDIR /app

# ...
